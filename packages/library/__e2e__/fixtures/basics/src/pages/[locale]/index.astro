---
export { getLocalePaths as getStaticPaths } from "@lightnet/library/i18n"

import { Gallery, Page, Section } from "@lightnet/library/components"
import { getMediaItems } from "@lightnet/library/content"

const allItems = await getMediaItems({})
---

<Page>
  <Section title={Astro.locals.t("home.all-items")}>
    <Gallery items={allItems} layout="book" />
  </Section>
</Page>
