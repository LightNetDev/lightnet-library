---
import { getMediaItem } from "../../../content/get-media-items"

interface Props {
  slug: string
}

const item = await getMediaItem(Astro.props.slug)
const authors = item.data.authors?.join(", ")
---

{authors && <p class="mt-2 text-xl sm:text-2xl">{authors}</p>}
