---
import { getMediaItem } from "../../../content/get-media-items"

interface Props {
  slug: string
}

const item = await getMediaItem(Astro.props.slug)
---

<h1
  class="text-center text-3xl font-bold sm:text-4xl md:text-start md:text-5xl"
  lang={item.data.language}
>
  {item.data.title}
</h1>
